---
title: "ری انفورسمنٹ لرننگ کے اصول (اردو ایڈوانسڈ)"
sidebar_position: 4
---

# ری انفورسمنٹ لرننگ کے اصول (اردو ایڈوانسڈ ورژن)

## باب کا جائزہ

ری انفورسمنٹ لرننگ (RL) ایک ریاضیاتی فریم ورک کی نمائندگی کرتا ہے جو بہترین کنٹرول تھیوری، ڈائنامک پروگرامنگ، اور مارکوف فیصلہ سازی کے عمل کی بنیاد پر ہے تاکہ غیر یقینی کے تحت تسلسل کے فیصلے کیے جا سکیں۔ نظریاتی بنیادوں میں بیلمن کے بہترین اصول، ٹمپورل ڈفیو لرننگ کے تقابلی تجزіہ، اور فنکشن تقرب کے خلاء میں پالیسی گریڈیئنٹس کی ریاضیاتی فارمولیشن شامل ہے۔ اعلیٰ RL میں سٹوکاسٹک آپٹیمائزیشن، معلوماتی تھیوری، اور گیم تھیوری کو ملانا شامل ہے تاکہ پیچیدہ فیصلہ سازی کے مسائل کے لیے بنیادی الگورتھم تیار کیے جا سکیں۔

### ریاضیاتی بنیادیں

اس بنیادی RL مسئلہ کو ایک مارکوف فیصلہ سازی کے عمل (MDP) کی طرح فارمولیٹ کیا جا سکتا ہے:

M = (S, A, P, R, γ, μ)

جہاں:
- S: حالت کی جگہ (ممکنہ طور پر جاری اور لامحدود)
- A: ایکشن کی جگہ (ڈسکریٹ یا کنٹی نیوئس)
- P: منتقلی کی امکانات P(s'|s,a) = Pr[S_{t+1} = s' | S_t = s, A_t = a]
- R: انعام فنکشن R(s,a,s')
- γ: ڈسکاؤنٹ فیکٹر γ ∈ [0,1]
- μ: ابتدائی حالت کی تقسیم

میٹا یہ ہے کہ ایک بہترین پالیسی π* تلاش کریں جو متوقع ڈسکاؤنٹ کمائی کو زیادہ سے زیادہ کرے:

J(π) = E[Σ_{t=0}^∞ γ^t R(s_t, a_t) | π]

### رسمی فریم ورک

بہترین ویلیو فنکشن کے لیے بیلمن کا مساوات:

V*(s) = max_a E[R(s,a,S') + γ V*(S') | S=s, A=a]

اور اس کے مطابق بہترین ایکشن-ویلیو فنکشن:

Q*(s,a) = E[R(s,a,S') + γ max_{a'} Q*(S', a') | S=s, A=a]

## اعلیٰ RL الگورتھم

### ٹمپورل ڈفیو لرننگ

ٹمپورل ڈفیو طریقے مونٹی کارلو اور ڈائنامک پروگرامنگ کے انداز میں ملانے کرتے ہیں:

**TD(0) اپ ڈیٹ قاعدہ**::
V(S_t) ← V(S_t) + α[R_{t+1} + γV(S_{t+1}) - V(S_t)]

**TD(λ) ایلیگیبلٹی ٹریس کے ساتھ**::
E_t(s) = λγE_{t-1}(s) + ∇_θ V(s;θ) اگر S_t = s، ورنہ 0
θ ← θ + αδ_tE_t

### سارسا اور Q-لرننگ

**سارسا (آن-پالیسی TD کنٹرول)**::
Q(S_t, A_t) ← Q(S_t, A_t) + α[R_{t+1} + γQ(S_{t+1}, A_{t+1}) - Q(S_t, A_t)]

**Q-لرننگ (آف-پالیسی TD کنٹرول)**::
Q(S_t, A_t) ← Q(S_t, A_t) + α[R_{t+1} + γmax_a Q(S_{t+1}, a) - Q(S_t, A_t)]

### پالیسی گریڈیئنٹ طریقے

پالیسی گریڈیئنٹ طریقے گریڈیئنٹ ایسکینٹ کے ذریعے براہ راست پالیسی کو اصلاح کرتے ہیں:

J(θ) = E_π_θ[G_0] = ∫_s d^{π_θ}(s) ∫_a π_θ(a|s) Q^{π_θ}(s,a) da ds

جہاں d^{π_θ}(s) پالیسی π_θ کے تحت مستقل حالت کی تقسیم ہے۔

**REINFORCE الگورتھم**::
∇_θ J(θ) = E[Σ_{t=0}^T ∇_θ log π_θ(A_t|S_t) G_t]

**ایکٹر-کریٹک طریقے**::
∇_θ J(θ) ≈ E[∇_θ log π_θ(A|S) Q_w(S,A)]

### ایکٹر-کریٹک آرکیٹیکچر

```python
import torch
import torch.nn as nn
import torch.nn.functional as F

class ActorCritic(nn.Module):
    def __init__(self, state_dim, action_dim, hidden_dim=256):
        super(ActorCritic, self).__init__()
        
        # مشترکہ خصوصیت نکالنے والا
        self.shared_layers = nn.Sequential(
            nn.Linear(state_dim, hidden_dim),
            nn.ReLU(),
            nn.Linear(hidden_dim, hidden_dim),
            nn.ReLU()
        )
        
        # ایکٹر: ایکشن کی امکانات (ڈسکریٹ کے لیے) یا مین/Std (کنٹی نیوئس کے لیے) دیتے ہیں
        self.actor_mean = nn.Linear(hidden_dim, action_dim)
        self.actor_std = nn.Parameter(torch.ones(action_dim) * 0.1)
        
        # کریٹک: حالت کی ویلیو دیتا ہے
        self.critic = nn.Linear(hidden_dim, 1)
    
    def forward(self, state):
        features = self.shared_layers(state)
        
        # ایکٹر: ایکشن تقسیم کے پیرامیٹر حساب
        action_mean = self.actor_mean(features)
        action_std = F.softplus(self.actor_std)  # مثبت یقین دہانی
        
        # کریٹک: حالت کی ویلیو حساب
        state_value = self.critic(features)
        
        return action_mean, action_std, state_value

class AdvantageActorCritic(nn.Module):
    def __init__(self, state_dim, action_dim, hidden_dim=256):
        super(AdvantageActorCritic, self).__init__()
        
        # مشترکہ لیئرز
        self.feature_extractor = nn.Sequential(
            nn.Linear(state_dim, hidden_dim),
            nn.ReLU(),
            nn.Linear(hidden_dim, hidden_dim),
            nn.ReLU()
        )
        
        # ایکٹر نیٹ ورک
        self.actor = nn.Sequential(
            nn.Linear(hidden_dim, hidden_dim),
            nn.ReLU(),
            nn.Linear(hidden_dim, action_dim),
            nn.Softmax(dim=-1)  # ڈسکریٹ ایکشنز کے لیے
        )
        
        # کریٹک نیٹ ورک
        self.critic = nn.Sequential(
            nn.Linear(hidden_dim, hidden_dim),
            nn.ReLU(),
            nn.Linear(hidden_dim, 1)
        )
    
    def forward(self, state):
        features = self.feature_extractor(state)
        action_probs = self.actor(features)
        state_value = self.critic(features)
        return action_probs, state_value
```

### ڈیپ Q-نیٹ ورک (DQN) اور اضافیات

```python
class DeepQNetwork(nn.Module):
    def __init__(self, state_dim, action_dim, hidden_dim=256):
        super(DeepQNetwork, self).__init__()
        
        self.q_network = nn.Sequential(
            nn.Linear(state_dim, hidden_dim),
            nn.ReLU(),
            nn.Linear(hidden_dim, hidden_dim),
            nn.ReLU(),
            nn.Linear(hidden_dim, action_dim)
        )
    
    def forward(self, state):
        return self.q_network(state)

class DoubleDQN(nn.Module):
    def __init__(self, state_dim, action_dim, hidden_dim=256):
        super(DoubleDQN, self).__init__()
        
        # دو نیٹ ورکس: آن لائن اور ہدف
        self.online_network = DeepQNetwork(state_dim, action_dim, hidden_dim)
        self.target_network = DeepQNetwork(state_dim, action_dim, hidden_dim)
        
        # ابتدائی طور پر وزن کاپی کریں
        self.target_network.load_state_dict(self.online_network.state_dict())
    
    def forward(self, state, network='online'):
        if network == 'online':
            return self.online_network(state)
        elif network == 'target':
            return self.target_network(state)

class DuelingDQN(nn.Module):
    def __init__(self, state_dim, action_dim, hidden_dim=256):
        super(DuelingDQN, self).__init__()
        
        # مشترکہ خصوصیت لیئر
        self.feature_layer = nn.Sequential(
            nn.Linear(state_dim, hidden_dim),
            nn.ReLU(),
            nn.Linear(hidden_dim, hidden_dim),
            nn.ReLU()
        )
        
        # ویلیو سٹریم
        self.value_stream = nn.Sequential(
            nn.Linear(hidden_dim, hidden_dim),
            nn.ReLU(),
            nn.Linear(hidden_dim, 1)
        )
        
        # فائدہ سٹریم
        self.advantage_stream = nn.Sequential(
            nn.Linear(hidden_dim, hidden_dim),
            nn.ReLU(),
            nn.Linear(hidden_dim, action_dim)
        )
    
    def forward(self, state):
        features = self.feature_layer(state)
        
        # ویلیو اور فائدہ حساب
        value = self.value_stream(features)
        advantages = self.advantage_stream(features)
        
        # ڈویل آرکیٹیکچر کا استعمال کرکے ضم کریں
        q_values = value + (advantages - advantages.mean(dim=1, keepdim=True))
        return q_values
```

## اعلیٰ ترین ایکسپلوریشن کی حکمت عملیں

### اوپری یقین کی حد (UCB) اور تھامسن سیمپلنگ

```python
class UCBExploration:
    def __init__(self, n_actions, c=2):
        self.n_actions = n_actions
        self.c = c  # ایکسپلوریشن پیرامیٹر
        self.action_counts = np.zeros(n_actions)
        self.action_values = np.zeros(n_actions)
        self.total_steps = 0
    
    def select_action(self, state):
        self.total_steps += 1
        
        # سب سے زیادہ UCB ویلیو والے ایکشن کا انتخاب کریں
        ucb_values = np.zeros(self.n_actions)
        for a in range(self.n_actions):
            if self.action_counts[a] == 0:
                return a  # غیر سیکھے ہوئے ایکشنز پہلے آزمائیں
            
            ucb_values[a] = (self.action_values[a] + 
                           self.c * np.sqrt(np.log(self.total_steps) / self.action_counts[a]))
        
        return np.argmax(ucb_values)

class ThompsonSampling:
    def __init__(self, n_actions):
        self.n_actions = n_actions
        # ہر ایکشن کے لیے بیٹا تقسیم کے پیرامیٹر (الفا، بیٹا)
        self.alpha = np.ones(n_actions)
        self.beta = np.ones(n_actions)
    
    def select_action(self, state):
        # ہر ایکشن کے لیے بیٹا تقسیم سے نمونہ لیں
        samples = [np.random.beta(self.alpha[a], self.beta[a]) for a in range(self.n_actions)]
        return np.argmax(samples)
    
    def update(self, action, reward):
        # انعام کی بنیاد پر بیٹا تقسیم کو اپ ڈیٹ کریں (0 یا 1)
        self.alpha[action] += reward
        self.beta[action] += (1 - reward)
```

### ذاتی محرک اور کیوریوسٹی-ڈرائیو لرننگ

```python
class IntrinsicMotivation:
    def __init__(self, state_dim, action_dim, hidden_dim=256, lr=1e-4):
        # فارورڈ ماڈل: موجودہ حالت اور ایکشن کے ذریعے اگلی حالت کی پیشن گوئی
        self.forward_model = nn.Sequential(
            nn.Linear(state_dim + action_dim, hidden_dim),
            nn.ReLU(),
            nn.Linear(hidden_dim, hidden_dim),
            nn.ReLU(),
            nn.Linear(hidden_dim, state_dim)
        )
        
        # انورس ماڈل: حالت کی منتقلی کے ذریعے ایکشن کی پیشن گوئی
        self.inverse_model = nn.Sequential(
            nn.Linear(2 * state_dim, hidden_dim),
            nn.ReLU(),
            nn.Linear(hidden_dim, hidden_dim),
            nn.ReLU(),
            nn.Linear(hidden_dim, action_dim)
        )
        
        self.forward_optimizer = torch.optim.Adam(self.forward_model.parameters(), lr=lr)
        self.inverse_optimizer = torch.optim.Adam(self.inverse_model.parameters(), lr=lr)
        
        self.intrinsic_scale = 0.1
    
    def compute_intrinsic_reward(self, state, action, next_state):
        """پیشن گوئی کی خامی کی بنیاد پر کیوریوسٹی بونس حساب کریں"""
        # فارورڈ ماڈل کے ذریعے اگلی حالت کی پیشن گوئی کریں
        state_action = torch.cat([state, action], dim=-1)
        predicted_next_state = self.forward_model(state_action)
        
        # پیشن گوئی کی خامی (کیوریوسٹی) کا حساب
        error = (predicted_next_state - next_state).pow(2).mean(dim=-1)
        
        return self.intrinsic_scale * error
    
    def update_models(self, states, actions, next_states):
        """فارورڈ اور انورس ماڈلز کو اپ ڈیٹ کریں"""
        # فارورڈ ماڈل کو اپ ڈیٹ کریں
        state_action = torch.cat([states, actions], dim=-1)
        predicted_next_states = self.forward_model(state_action)
        forward_loss = F.mse_loss(predicted_next_states, next_states)
        
        self.forward_optimizer.zero_grad()
        forward_loss.backward()
        self.forward_optimizer.step()
        
        # انورس ماڈل کو اپ ڈیٹ کریں
        state_pairs = torch.cat([states, next_states], dim=-1)
        predicted_actions = self.inverse_model(state_pairs)
        inverse_loss = F.mse_loss(predicted_actions, actions)
        
        self.inverse_optimizer.zero_grad()
        inverse_loss.backward()
        self.inverse_optimizer.step()
```

## ماڈل-مبنی RL

### Dyna آرکیٹیکچر

Dyna آرکیٹیکچر حقیقی تجربے کو ماڈل-مبنی منصوبہ بندی کے ساتھ جوڑتا ہے:

```python
class DynaAgent:
    def __init__(self, model, planner, real_env, n_simulated_updates=10):
        self.model = model  # سیکھا ہوا ماحول کا ماڈل
        self.planner = planner  # منصوبہ بندی الگورتھم (مثلاً ویلیو ایٹریشن)
        self.real_env = real_env
        self.n_simulated_updates = n_simulated_updates
    
    def train_step(self, state, action, reward, next_state, done):
        # حقیقی تعامل کے ساتھ ماڈل کو اپ ڈیٹ کریں
        self.model.update(state, action, reward, next_state, done)
        
        # حقیقی تجربے کے ساتھ پالیسی کو اپ ڈیٹ کریں
        self.update_policy_real(state, action, reward, next_state, done)
        
        # سیکھے ہوئے ماڈل کے استعمال سے مشابہ اپ ڈیٹس کریں
        for _ in range(self.n_simulated_updates):
            # ماڈل سے تجربہ نمونہ لیں
            sim_state = self.model.sample_state()
            sim_action = self.select_action(sim_state)  # موجودہ پالیسی کا پیرو کریں
            sim_reward, sim_next_state = self.model.predict(sim_state, sim_action)
            
            # مشابہ تجربے کے ساتھ پالیسی کو اپ ڈیٹ کریں
            self.update_policy_simulated(sim_state, sim_action, sim_reward, sim_next_state)

class WorldModel(nn.Module):
    def __init__(self, state_dim, action_dim):
        super(WorldModel, self).__init__()
        
        # حالت کی منتقلی ماڈل
        self.transition_net = nn.Sequential(
            nn.Linear(state_dim + action_dim, 256),
            nn.ReLU(),
            nn.Linear(256, 256),
            nn.ReLU(),
            nn.Linear(256, state_dim)
        )
        
        # انعام ماڈل
        self.reward_net = nn.Sequential(
            nn.Linear(state_dim + action_dim, 256),
            nn.ReLU(),
            nn.Linear(256, 256),
            nn.ReLU(),
            nn.Linear(256, 1)
        )
    
    def forward(self, state, action):
        state_action = torch.cat([state, action], dim=-1)
        next_state_delta = self.transition_net(state_action)
        next_state = state + next_state_delta  # ریزیڈوئل اپ ڈیٹ
        reward = self.reward_net(state_action)
        return next_state, reward
    
    def predict(self, state, action):
        with torch.no_grad():
            next_state, reward = self.forward(state, action)
        return reward.item(), next_state
```

### ماڈل پریڈکٹو کنٹرول (MPC) انضمام

```python
class MPCPlanner:
    def __init__(self, model, horizon=10, n_candidates=1000):
        self.model = model
        self.horizon = horizon
        self.n_candidates = n_candidates
    
    def plan(self, initial_state, goal_state):
        best_action_sequence = None
        best_return = float('-inf')
        
        for _ in range(self.n_candidates):
            # بے ترتیب ایکشن سیکوئنس نمونہ لیں
            action_sequence = torch.randn(self.horizon, initial_state.shape[-1])
            action_sequence = torch.tanh(action_sequence)  # [-1, 1] میں کلیمپ کریں
            # ٹریجکٹری کی شبیہہ کاری کریں
            state = initial_state.clone()
            total_return = 0
            
            for t in range(self.horizon):
                # ماڈل کا استعمال کرکے اگلی حالت کی پیشن گوئی کریں
                next_state, reward = self.model(state.unsqueeze(0), 
                                               action_sequence[t].unsqueeze(0))
                
                # انعام کا حساب (مقصد سے فاصلہ)
                goal_reward = -torch.norm(next_state[:2] - goal_state[:2])
                total_return += (0.9 ** t) * (reward + goal_reward)
                
                state = next_state.squeeze(0)
            
            if total_return > best_return:
                best_return = total_return
                best_action_sequence = action_sequence.clone()
        
        # بہترین سیکوئنس کا پہلا ایکشن واپس کریں
        return best_action_sequence[0] if best_action_sequence is not None else None
```

## اعلیٰ ترین پالیسی آپٹیمائزیشن

### ٹرسٹ ریجن پالیسی آپٹیمائزیشن (TRPO)

TRPO بڑی پالیسی تبدیلیوں کو روکنے کے لیے پالیسی اپ ڈیٹس کو محدود کرتا ہے:

```python
def trpo_compute_kl_divergence(pi_old, pi_new, states, actions):
    """پرانی اور نئی پالیسیز کے درمیان KL تقسیم کا حساب کریں"""
    with torch.no_grad():
        log_prob_old = pi_old.get_log_prob(states, actions)
    log_prob_new = pi_new.get_log_prob(states, actions)
    return (log_prob_old - log_prob_new).mean()

def trpo_line_search(f, x, fullstep, expected_improve_rate):
    """TRPO کے لیے لائن سرچ کریں"""
    accept_ratio = 0.1
    max_backtracks = 10
    for (_n_backtracks, stepfrac) in enumerate(.5 ** np.arange(max_backtracks)):
        x_new = x - stepfrac * fullstep
        improve = f(x) - f(x_new)
        if improve >= 0 and stepfrac * expected_improve_rate >= improve * accept_ratio:
            return x_new
    return x
```

### پروکسیمل پالیسی آپٹیمائزیشن (PPO)

PPO TRPO کو سادہ کلپنگ میکنزم کے ساتھ ت_approximate_ کرتا ہے:

```python
class PPO:
    def __init__(self, policy, value_net, clip_epsilon=0.2, lr=3e-4):
        self.policy = policy
        self.value_net = value_net
        self.clip_epsilon = clip_epsilon
        self.policy_optimizer = torch.optim.Adam(policy.parameters(), lr=lr)
        self.value_optimizer = torch.optim.Adam(value_net.parameters(), lr=lr)
    
    def compute_loss(self, states, actions, old_log_probs, returns, advantages):
        # نیا ایکشن امکانات کا حساب
        new_log_probs, entropy = self.policy.get_log_prob_and_entropy(states, actions)
        
        # تناسب کا حساب
        ratio = torch.exp(new_log_probs - old_log_probs)
        
        # PPO سرروگیٹ نقصان
        surr1 = ratio * advantages
        surr2 = torch.clamp(ratio, 1.0 - self.clip_epsilon, 1.0 + self.clip_epsilon) * advantages
        policy_loss = -torch.min(surr1, surr2).mean() - 0.01 * entropy.mean()
        
        # ویلیو نقصان
        values = self.value_net(states)
        value_loss = F.mse_loss(values, returns)
        
        return policy_loss, value_loss
    
    def update(self, states, actions, old_log_probs, returns, advantages):
        policy_loss, value_loss = self.compute_loss(states, actions, old_log_probs, returns, advantages)
        
        # پالیسی کو اپ ڈیٹ کریں
        self.policy_optimizer.zero_grad()
        policy_loss.backward()
        torch.nn.utils.clip_grad_norm_(self.policy.parameters(), 0.5)
        self.policy_optimizer.step()
        
        # ویلیو فنکشن کو اپ ڈیٹ کریں
        self.value_optimizer.zero_grad()
        value_loss.backward()
        torch.nn.utils.clip_grad_norm_(self.value_net.parameters(), 0.5)
        self.value_optimizer.step()
```

### سافٹ ایکٹر-کریٹک (SAC)

SAC ایک آف پالیسی زیادہ سے زیادہ اینٹروپی الگورتھم ہے:

```python
class SoftActorCritic:
    def __init__(self, state_dim, action_dim, hidden_dim=256, alpha=0.2):
        # ایکٹر (پالیسی نیٹ ورک)
        self.actor = GaussianPolicy(state_dim, action_dim, hidden_dim)
        
        # کریٹک نیٹ ورکس (Q-فنکشنز)
        self.critic1 = QNetwork(state_dim, action_dim, hidden_dim)
        self.critic2 = QNetwork(state_dim, action_dim, hidden_dim)
        
        # ہدف نیٹ ورکس
        self.critic1_target = copy.deepcopy(self.critic1)
        self.critic2_target = copy.deepcopy(self.critic2)
        
        # آپٹیمائزرز
        self.actor_optimizer = torch.optim.Adam(self.actor.parameters(), lr=3e-4)
        self.critic_optimizer = torch.optim.Adam(
            list(self.critic1.parameters()) + list(self.critic2.parameters()), 
            lr=3e-4
        )
        
        # اینٹروپی ٹمپریچر
        self.target_entropy = -torch.prod(torch.Tensor(action_dim)).item()
        self.log_alpha = torch.zeros(1, requires_grad=True)
        self.alpha_optimizer = torch.optim.Adam([self.log_alpha], lr=3e-4)
        self.alpha = alpha
    
    def update(self, replay_buffer, batch_size=256):
        state, action, reward, next_state, done = replay_buffer.sample(batch_size)
        
        # ہدف Q-ویلیوز کا حساب
        with torch.no_grad():
            next_action, next_log_prob, _ = self.actor.sample(next_state)
            next_q1 = self.critic1_target(next_state, next_action)
            next_q2 = self.critic2_target(next_state, next_action)
            next_q = torch.min(next_q1, next_q2) - self.alpha * next_log_prob
            target_q = reward + (1 - done) * 0.99 * next_q
        
        # کریٹک اپ ڈیٹ
        current_q1 = self.critic1(state, action)
        current_q2 = self.critic2(state, action)
        critic_loss = F.mse_loss(current_q1, target_q) + F.mse_loss(current_q2, target_q)
        
        self.critic_optimizer.zero_grad()
        critic_loss.backward()
        self.critic_optimizer.step()
        
        # ایکٹر اپ ڈیٹ
        pi, log_prob, _ = self.actor.sample(state)
        q1_pi = self.critic1(state, pi)
        q2_pi = self.critic2(state, pi)
        min_q_pi = torch.min(q1_pi, q2_pi)
        
        actor_loss = (self.alpha * log_prob - min_q_pi).mean()
        
        self.actor_optimizer.zero_grad()
        actor_loss.backward()
        self.actor_optimizer.step()
        
        # الفا اپ ڈیٹ
        alpha_loss = -(self.log_alpha * (log_prob + self.target_entropy).detach()).mean()
        self.alpha_optimizer.zero_grad()
        alpha_loss.backward()
        self.alpha_optimizer.step()
        self.alpha = self.log_alpha.exp()
```

## ملٹی-ایجنٹ RL

### انڈیپنڈنٹ Q-لرننگ

سادہ طریقہ جہاں ہر ایجنٹ آزادانہ طور پر سیکھتا ہے:

```python
class IndependentQLearning:
    def __init__(self, n_agents, state_dim, action_dim, hidden_dim=256):
        self.agents = [DQN(state_dim, action_dim, hidden_dim) 
                      for _ in range(n_agents)]
        self.n_agents = n_agents
    
    def get_actions(self, states):
        """تمام ایجنٹس کے لیے ایکشن حاصل کریں"""
        actions = []
        for i in range(self.n_agents):
            q_values = self.agents[i](states[i].unsqueeze(0))
            action = torch.argmax(q_values, dim=1)
            actions.append(action.item())
        return actions
    
    def update(self, agent_idx, state, action, reward, next_state, done):
        """انفرادی ایجنٹ کے Q-نیٹ ورک کو اپ ڈیٹ کریں"""
        self.agents[agent_idx].update(state, action, reward, next_state, done)
```

### سنٹرلائزڈ ٹریننگ کے ساتھ ڈیسینٹرلائزڈ ایکسیکیوشن (CTDE)

```python
class MADDPG:
    """ملٹی-ایجنٹ ڈیپ ڈیٹرمنسٹک پالیسی گریڈیئنٹ"""
    def __init__(self, n_agents, state_dims, action_dims, hidden_dim=256):
        self.n_agents = n_agents
        self.actors = [DDPGActor(state_dims[i], action_dims[i], hidden_dim) 
                      for i in range(n_agents)]
        self.critics = [DDPGCritic(sum(state_dims), sum(action_dims), hidden_dim) 
                       for _ in range(n_agents)]
        
        # ہدف نیٹ ورکس
        self.target_actors = [copy.deepcopy(actor) for actor in self.actors]
        self.target_critics = [copy.deepcopy(critic) for critic in self.critics]
    
    def get_actions(self, states, add_noise=True):
        """تمام ایجنٹس کے لیے ایکشن حاصل کریں (ڈیسینٹرلائزڈ ایکسیکیوشن)"""
        actions = []
        for i in range(self.n_agents):
            action = self.actors[i](states[i].unsqueeze(0))
            if add_noise:
                noise = torch.randn_like(action) * 0.1
                action += noise
            actions.append(action.clamp(-1, 1))
        return actions
    
    def centralized_critic_training(self, states, actions, rewards, next_states):
        """سینٹرلائزڈ معلومات کا استعمال کرکے تربیت (CTDE)"""
        all_actions = torch.cat(actions, dim=1)
        all_states = torch.cat(states, dim=1)
        
        # سنٹرلائزڈ معلومات کا استعمال کرکے کریٹکس کو اپ ڈیٹ کریں
        for i in range(self.n_agents):
            next_actions = []
            for j in range(self.n_agents):
                next_action = self.target_actors[j](next_states[j])
                next_actions.append(next_action)
            all_next_actions = torch.cat(next_actions, dim=1)
            all_next_states = torch.cat(next_states, dim=1)
            
            with torch.no_grad():
                target_q = rewards[i] + 0.99 * self.target_critics[i](
                    all_next_states, all_next_actions
                )
            
            current_q = self.critics[i](all_states, all_actions)
            critic_loss = F.mse_loss(current_q, target_q.detach())
            
            self.critics[i].optimizer.zero_grad()
            critic_loss.backward()
            self.critics[i].optimizer.step()
```

## RL میں اعلیٰ ترین موضوعات

### ہیرارکیکل RL

ہیرارکیکل RL پیچیدہ کاموں کو ذیلی کاموں میں تقسیم کرتا ہے:

```python
class OptionCritic:
    """ہیرارکیکل RL کے لیے آپشن-کریٹک آرکیٹیکچر"""
    def __init__(self, state_dim, action_dim, n_options, hidden_dim=256):
        # ختم کرنے والی حالت نیٹ ورک
        self.termination_net = nn.Sequential(
            nn.Linear(state_dim, hidden_dim),
            nn.ReLU(),
            nn.Linear(hidden_dim, n_options),
            nn.Sigmoid()
        )
        
        # آپشن پالیسی نیٹ ورک
        self.option_policy = nn.Sequential(
            nn.Linear(state_dim + n_options, hidden_dim),  # + آپشن شناخت کار
            nn.ReLU(),
            nn.Linear(hidden_dim, action_dim),
            nn.Softmax(dim=-1)
        )
        
        # آپشنز کے لیے Q-فنکشن
        self.q_option = nn.Sequential(
            nn.Linear(state_dim, hidden_dim),
            nn.ReLU(),
            nn.Linear(hidden_dim, n_options)
        )
    
    def forward(self, state, option):
        """مخصوص آپشن کے لیے فارورڈ پاس"""
        state_option = torch.cat([state, F.one_hot(option, num_classes=self.n_options)], dim=-1)
        action_probs = self.option_policy(state_option)
        termination_probs = self.termination_net(state)
        q_values = self.q_option(state)
        
        return action_probs, termination_probs, q_values
```

### ڈسٹری بیوشنل RL

ڈسٹری بیوشنل RL فقط توقعات کے بجائے واپسیوں کے مکمل تقسیم کو سیکھتا ہے:

```python
class CategoricalDQN(nn.Module):
    """زمرہ وار DQN - واپسیوں کے تقسیم پر سیکھنا"""
    def __init__(self, state_dim, action_dim, n_atoms=51, v_min=-10.0, v_max=10.0):
        super(CategoricalDQN, self).__init__()
        self.n_atoms = n_atoms
        self.v_min = v_min
        self.v_max = v_max
        self.action_dim = action_dim
        
        # زمرہ وار تقسیم کے لیے سپورٹ
        self.register_buffer('support', torch.linspace(v_min, v_max, n_atoms))
        self.delta_z = (v_max - v_min) / (n_atoms - 1)
        
        # نیٹ ورک ایٹمس پر امکانات کی تقسیم آؤٹ پٹ کرتا ہے
        self.network = nn.Sequential(
            nn.Linear(state_dim, 512),
            nn.ReLU(),
            nn.Linear(512, 512),
            nn.ReLU(),
            nn.Linear(512, action_dim * n_atoms)
        )
    
    def forward(self, state):
        logits = self.network(state)
        logits = logits.view(-1, self.action_dim, self.n_atoms)
        probs = F.softmax(logits, dim=-1)
        return probs
    
    def compute_q_values(self, state):
        """تقسیم سے متوقع Q-ویلیوز حساب کریں"""
        probs = self.forward(state)
        q_values = torch.sum(probs * self.support, dim=2)
        return q_values
```

## نمونہ کارکردگی اور جنرلائزیشن

### تجربہ ریپلے کی تکنیکس

```python
class PrioritizedExperienceReplay:
    def __init__(self, capacity, alpha=0.6, beta_start=0.4):
        self.capacity = capacity
        self.alpha = alpha
        self.beta = beta_start
        self.memory = []
        self.priorities = []
        self.position = 0
    
    def push(self, state, action, reward, next_state, done, priority=None):
        if len(self.memory) < self.capacity:
            self.memory.append(None)
            self.priorities.append(None)
        
        if priority is None:
            priority = 1.0  # ڈیفالٹ ترجیح
        
        self.memory[self.position] = (state, action, reward, next_state, done)
        self.priorities[self.position] = priority
        self.position = (self.position + 1) % self.capacity
    
    def sample(self, batch_size, beta=None):
        if beta is None:
            beta = self.beta
        
        # امکانات کا حساب
        total = len(self.memory)
        priorities = np.array(self.priorities)
        probabilities = priorities ** self.alpha
        probabilities /= probabilities.sum()
        indices = np.random.choice(total, batch_size, p=probabilities)
        
        # امپورٹنس سیمپلنگ کا وزن حساب
        weights = (total * probabilities[indices]) ** (-beta)
        weights /= weights.max()
        
        experiences = [self.memory[idx] for idx in indices]
        return experiences, indices, weights
    
    def update_priorities(self, batch_indices, td_errors):
        """TD ایرر کی بنیاد پر ترجیحات کو اپ ڈیٹ کریں"""
        for idx, td_error in zip(batch_indices, td_errors):
            priority = td_error + 1e-5  # بہت کم ایپسیلون تاکہ صفر ترجیح سے بچا جا سکے
            self.priorities[idx] = priority ** self.alpha
```

## جائزہ اور تجزیہ

### آف-پالیسی جائزہ

دیگر پالیسی سے ڈیٹا کا استعمال کرکے پالیسی کا جائزہ لینے کی تکنیکس:

```python
def importance_sampling_evaluation(behavior_policy_probs, target_policy_probs, 
                                  rewards, discounts):
    """امپورٹنس سیمپلنگ کا استعمال کرکے آف-پالیسی جائزہ"""
    # امپورٹنس کے وزن حساب
    weights = target_policy_probs / behavior_policy_probs
    weights = torch.clamp(weights, 0, 10)  # حد سے بچنے کے لیے
    
    # وزن دی ہوئی واپسیاں کا حساب
    weighted_returns = []
    for i, (r, d) in enumerate(zip(rewards, discounts)):
        accumulated = 0
        for j in range(i, len(rewards)):
            accumulated = r + d * accumulated
            if j == i:
                break
        weighted_returns.append(weights[i] * accumulated)
    
    return torch.mean(torch.tensor(weighted_returns))

def doubly_robust_estimation(behavior_policy_probs, target_policy_probs, rewards, 
                            q_values, discounts):
    """ڈبلی روبسٹ آف-پالیسی جائزہ"""
    # امپورٹنس سیمپلنگ ٹرم
    rho = target_policy_probs / behavior_policy_probs
    rho = torch.clamp(rho, 0, 10)
    
    # براہ راست طریقہ ٹرم
    dm_term = target_policy_probs * q_values
    
    # ڈبلی روبسٹ اسٹیمیٹر
    dr_estimate = rho * (rewards + discounts * q_values) + dm_term - rho * q_values
    return torch.mean(dr_estimate)
```

## کلیدی نکات

- RL بہترین کنٹرول تھیوری اور ڈائنامک پروگرامنگ پر مبنی ہے
- اعلیٰ الگورتھم پالیسی گریڈیئنٹس، ایکٹر-کریٹک، اور ڈسٹری بیوشنل RL شامل ہیں
- ایکسپلوریشن کی حکمت عملی عدم یقینی اور پہلے کے علم کا توازن برقرار رکھتی ہے
- ماڈل-مبنی طریقے سیکھے ہوئے ماحول کے ماڈلز کو منصوبہ بندی کے لیے استعمال کرتے ہیں
- ملٹی-ایجنٹ RL ایجنٹس کے درمیان مطابقت اور مقابلہ کو سنبھالتا ہے
- نمونہ کارکردگی حقیقی دنیا کے اطلاقات میں اب بھی ایک کلیدی چیلنج ہے
- جائزہ کو آف-پالیسی اثرات کا خیال رکھنے کی ضرورت ہوتی ہے

## جائزہ سوالات

1. بہترین ایکشن-ویلیو فنکشن Q*(s,a) کے لیے بیلمن مساوات کا انداز کریں۔
2. آن-پالیسی اور آف-پالیسی لرننگ میں فرق کو مثالوں کے ساتھ سمجھائیں۔
3. پالیسی گریڈیئنٹ تھیورم کی کلیدی ریاضیاتی ضمانتیں کیا ہیں؟
4. TRPO الگورتھم ریاضیاتی طور پر پالیسی اپ ڈیٹس کو کیسے محدود کرتا ہے؟
5. ڈسٹری بیوشنل RL کے تصور کو سمجھائیں اور اس کے توقع-ویلیو RL پر کیا فوائد ہیں؟